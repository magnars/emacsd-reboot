# -*- mode: snippet -*-
# name: MT do
# key: mtdo
# --
(do
  (def ctx (:system/ctx integrant.repl.state/system))
  (def app-db (d/db (:app/conn ctx)))
  (def matrikkel-db (d/db (:matrikkel/conn ctx)))
  (def stabil-db (d/db (:stabil/conn ctx))))