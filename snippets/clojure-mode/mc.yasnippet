# -*- mode: snippet -*-
# name: MT designsystemet
# key: mc
# --
`(when (ignore-errors (save-excursion (paredit-backward-up) (not (looking-at "{")))) "{")`:class (m/c :$0)`(when (ignore-errors (save-excursion (paredit-backward-up) (not (looking-at "{")))) "}")`